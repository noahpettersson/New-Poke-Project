import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        main: "#f00"
    },
    typography: {
        fontFamily: "DotGothic16"
    },
    textField: {
        width: "25px",
        margin: "10px"
    }
});
